# Ceiling Grid Studio - Dockerfile
# Multi-stage build for optimized production images
#
# Build: docker build -t ceiling-grid-studio -f infra/dockerfile .
# Run:   docker run -p 8080:80 ceiling-grid-studio